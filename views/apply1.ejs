<!DOCTYPE html>
<html lang="en">
    <head>
        <!-- Bootstrap core CSS -->
        <meta charset="utf-8" />
        <title>Apply for funding</title>
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous"/>
        <link rel="stylesheet" href="/public/index.css"/>
        <style>
            @import url('https://fonts.googleapis.com/css?family=Khand');
        </style>
        <!-- The paper plane icon for the submit button. -->
        <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.7.1/css/all.css" integrity="sha384-fnmOCqbTlWIlj8LyTjo7mOUStjsKC4pOpQbqyi7RrhN7udi9RwhKkMHpvLbHG9Sr" crossorigin="anonymous">
    </head>
    <body>
        <div class="jumbotron text-center" style="margin-bottom:0">
            <h1>Team 10 Research Page</h1>
            <p>This is the page where you can apply for funding.</p>
            <% if (status.length) { %> 
                <%= status %>
            <% } %>
            <% if (success.length) { %>
                <%= success %>
            <% } %>
        </div>
        <!-- To change nav colour change the bg-info part to be bg-somethingelse. Navbar-custom can also be changed for nice looking stuff. -->
        <% include mainNav %>
        <main class="container" style="margin-bottom:0">
            <h1>
                Applying for funding.
            </h1>
            <p>
                If you'd like to apply for your relavant funding<br> Please fill out the form below and attach a CV.
            </p>
            <form class="form-apply" method="post" action="/apply" role="form">
                    <div class="form-group">
                        <label for="name">Full name:</label>
                        <textarea class="form-control" rows="1" id="name" name="name" placeholder="Your Full name"><%= fullName %></textarea>
                    </div>
                    <!-- Remember the funding that is clicked on -->
                    <!--
                    <div class="form-group">
                        <label for="fundingID">Funding:</label>
                        <textarea class="form-control" rows="1" id="fundingID" name="fundingID" placeholder="Your Funding ID"></textarea>
                    </div>
                    -->
                    <input type = "hidden" name="fundingID" value = <%=call_id %> />
                    <div class="form-group">
                        <label for="amount_requested">Amount Requested:</label>
                        <textarea class="form-control" rows="1" id="amount_requested" name="amount_requested" placeholder="Amount"></textarea>
                    </div>
                    <div class="form-group">
                        <label for="cover_note">Why you should get this funding:</label>
                        <textarea class="form-control" rows="5" id="cover_note" name="cover_note" placeholder="Reason..."></textarea>
                    </div>
                    <div class="form-group">
                        <label for="group_name">Group Name:</label>
                        <textarea class="form-control" rows="1" id="group_name" name="group_name" placeholder="Group Name"></textarea>
                    </div>
                    <div class="file-upload-wrapper">
                        <label for="input-file-now" class="file-upload">Choose File</label>
                        <input type="file" id="input-file-now" name="input-file-now"/>
                    </div>
                    <div class = "row">
                        <button class="btn btn-lg btn-primary btn-block" type="submit"><i class="far fa-paper-plane"></i>Submit Application</button>
            </form>
        </main>
    </body>
    <% include mainFooter %>
</html>
